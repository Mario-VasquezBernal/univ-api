generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/@prisma/client-carreras"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_CARRERAS")
}

// ... resto de tus modelos (Carrera, Ciclo, Materia, Curso, Alumno, etc.)
